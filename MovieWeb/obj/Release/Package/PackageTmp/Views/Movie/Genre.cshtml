@model IEnumerable<MovieWeb.Models.TheLoai>

@foreach (var item in Model)
{
    <li class="dropdown"> <a href="@Url.Action("MovieToGenre","Movie", new {id = item.MaTheLoai})">@item.TenTheLoai</a> 
    </li>
    
}
